Pour utiliser l'api je vais sur le site: https://rapidapi.com/apininjas/api/cars-by-api-ninjas
subscribe => connexion (google ou autre) => endpoints

Je créé un dossier utils => fichier index.tsx
Puis copier les logins et infos nécessaire dans "copy-snippets"(à droite)
Mais je peux le transformer par:

export async function fetchCars() {
    //il faut copier les headers de notre compte(ceux ci dessus)
    const headers = {
        'X-RapidAPI-Key': '26fe4166e2msh8c482a1b1d4e213p1bb14cjsnbc867cdd8e2d',
        'X-RapidAPI-Host': 'cars-by-api-ninjas.p.rapidapi.com'
    };
    const response = await fetch("https://cars-by-api-ninjas.p.rapidapi.com/v1/cars", {
        headers: headers,
    });

    const result = await response.json();

    return result;
    // ?model=corolla
    //ceci sera mon endpoint pour chercher une voiture particulière
}

J'importe cette fonction de cette façon:
import { fetchCars } from '@/utils';

  const allCars = await fetchCars();
Dans ./app/page.tsx

ATTENTION !!!
je dois transformer ma fonction en fonction asynchrone en rajoutant le mot clé
async pour pouvoir utiliser await
export default async function Home() {}

Nous avons finalement rajouté "?model=corolla" comme endpoint, sinong aucune data n'était renvoyé
, à noter que le résultat s'affiche dans le terminal(j'ai 5 toyotas corollas), mais pas dans la console.
, on peut rémedier à cela en appelant "use client"; en haut de page, mais ce n'est pas nécessaire.